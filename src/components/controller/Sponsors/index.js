import { connect } from 'react-redux';
import View from 'components/view/Sponsors';

const mapStateToProps = (state) => {
    return {};
};

const SponsorsController = connect(
    mapStateToProps,
)(View);

export default SponsorsController;